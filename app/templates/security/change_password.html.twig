{% extends 'base.html.twig' %}

{% block title %}Réinitialiser mon mot de passe{% endblock %}

{% block body %}
    <section class="reset-form-section">
        <h1 class="title-new">Réinitialiser mon mot de passe</h1>

        {{ form_start(ChangePasswordForm) }}

        {# Affiche les erreurs générales du formulaire (ex: si les deux champs ne matchent pas) #}
        {{ form_errors(ChangePasswordForm) }}

        <div class="form-group">
            {{ form_label(ChangePasswordForm.password.first) }}
            {{ form_widget(ChangePasswordForm.password.first) }}
            {{ form_errors(ChangePasswordForm.password.first) }}
        </div>

        <div class="form-group">
            {{ form_label(ChangePasswordForm.password.second) }}
            {{ form_widget(ChangePasswordForm.password.second) }}
            {{ form_errors(ChangePasswordForm.password.second) }}
        </div>

        <div class="form-group">
            <button type="submit" class="btn-black">Envoyer</button>
        </div>

        {{ form_end(ChangePasswordForm) }}
    </section>
{% endblock %}

